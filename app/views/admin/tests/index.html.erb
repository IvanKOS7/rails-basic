<%= render inline: @badge %>
<h1> <%= t('.header') %></h1>
<p> <%= link_to 'badges', admin_badges_path %></p>
<p> <%= link_to t('.new_test'), new_admin_test_path %></p>
<%= render 'shared/errors', resource: @test  %>
<table class="table">
  <tr>
    <td class="sort-by-title pointer">
      <%= t('.title') %>
      <%= octicon 'arrow-up', class: 'text-success hide' %>
      <%= octicon 'arrow-down', class: 'text-success hide' %>
    </td>
    <td><%= t('.level') %></td>
    <td><%= t('.questions_amount') %></td>
    <td colspan="4" class="text-center"><%= t('.actions') %></td>
    <td colspan="2"> <%= t('.publication') %></td>
  </tr>
  <%= render @tests %>
</table>
