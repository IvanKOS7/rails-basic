
<table class="table">
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.questions_amount') %></th>
    <th>Timer</th>
    <th class="text-center" colspan="4"><%= t('.actions') %></th>
  </tr>
  <%= render @test %>
</table>
<table class="table">
  <tr>
    <th>Questions</th>
    <th colspan="3" class="text-center"><%= t('.actions') %></th>
  </tr>
  <%= render 'admin/questions/test_questions', resource: @test.questions %>
<table/>

<%= link_to t('.back'), admin_tests_path %>
