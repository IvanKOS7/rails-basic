<div class="topnav">
  <p>TestGuru</p>
  <a class="active" ><%= link_to t('.home'), tests_path %></a>
  <a href="/gurus/sign_up" class="active"><%= t('.sign_up') %></a>
  <a href="/gurus/login" class= "active"><%= t('.login') %></a>
  <a class="nav user">
    <% if user_signed_in? %>
    <p><%= t('.greeter', email: current_user.email) %></p>
    <%= link_to 'Exit', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }, class: "active" %>
  <% end %> </a>
</div>
