ru:
  activerecord:
    models:
      test: "Тест"
    attributes:
      test:
        title: "Название"
    errors:
      messages:
        blank: "не может быть пустым"
  services:
    description: "A question about  from TestGuru"      
